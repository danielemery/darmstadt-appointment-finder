{
  "name": "@danielemery/darmstadt-appointment-finder",
  "version": "0.0.1",
  "type": "module",
  "description": "Docker container to run on a CRON to check the Darmstadt public office for appointments.",
  "dependencies": {
    "playwright": "1.61.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0"
  },
  "scripts": {
    "start": "npm run start:dev",
    "start:prod": "node dist/main.js",
    "start:dev": "npm run build && node --env-file-if-exists=.env dist/main.js",
    "build": "tsc",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "lint:manifests": "kustomize build deploy > /dev/null",
    "test": "npm run build && node --test 'dist/**/*.test.js'",
    "verify": "npm run lint && npm run lint:manifests && npm run test"
  },
  "author": "Daniel Emery <daniel@demery.net>",
  "license": "MIT"
}
